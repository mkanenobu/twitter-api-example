{
  "name": "twitter-app-example",
  "private": true,
  "engines": {
    "npm": "Use pnpm instead of npm"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "npx only-allow pnpm"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "2.8.3",
    "typescript": "4.9.4"
  }
}
